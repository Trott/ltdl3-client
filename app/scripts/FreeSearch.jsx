/**
 * @jsx React.DOM
 */
var React = require('react');
var SearchTextBox = require('./SearchTextBox.jsx');

module.exports = React.createClass({
    render: function () {
        return (
            <SearchTextBox/>
        );
    }
});

